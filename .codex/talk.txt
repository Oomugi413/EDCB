EpgTimerの設定である自動予約登録に合わせて予約を変更するオプション、
SyncResAutoAddChange
SyncResAutoAddDelete
SyncResAutoAddChgNewRes
SyncResAutoAddChgKeepRecTag
をlinuxでも使えるようにする方法を考えて、
~/git/EDCB/.codex/linux_reserve_feature.md
に提案して。

現状、ファイルを書き換えないままではlinuxで通常使用は不可能であるという結論で大丈夫か。

~/git/EDCB/.codex/linux_reserve_feature.md
に基づくファイル変更を許可する。ただし、以下の条件に従うこと。

・それぞれの変更ファイルごとに、元ファイルはファイル名を変更して保持し、元ファイル（名称変更）と新ファイル（名称維持）の2つを作成。
・変更したファイルの新旧名称と、もとのコードへの復旧方法をまとめて
~/git/EDCB/.codex/linux_reserve_codediff.md
にまとめること。
・変更した点を、linux_reserve_feature.mdと同様のフォーマットで、
~/git/EDCB/.codex/linux_reserve_change.md
にまとめること。
・現在EDCBでは録画が進行中のため、**systemctlによるEDCBの再起動を絶対に行わない。**再起動を必要とするテストは、私（ユーザー）の承認後に行う。ただし、21時(JST)までは承認しない。

現在実施した変更で、例えば、
・放送局をすべて指定し、録画後実装batを指定しない
EPG予約を追加して、4つの番組が指定されたとする。その後、このEPG予約を
・放送局を1つ指定し、録画後実装batを指定する
EPG予約に変更した場合、
・3つの予約が削除
・1つの予約にbatが追加
となるようなオプションは
SyncResAutoAddChange=1
SyncResAutoAddDelete=1
SyncResAutoAddChgNewRes=1
SyncResAutoAddChgKeepRecTag=0
で間違いないか。

返答の内容は期待していた内容であり、修正は必要ない。
現在の設定で、先述のオプションの場合、
・複数のEPG予約によって番組予約がなっており、片方を削除した場合
の動作はどのようになるか。

codex resume 019f0871-fad3-7e02-9e8b-a0eb7c43fb62


